name: light_compressor_example
description: Demonstrates how to use the light_compressor plugin.
version: 0.0.1

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.10.5 <3.0.0"

dependencies:
  cupertino_icons:
  file_picker:
  permission_handler:
  flutter:
    sdk: flutter
  light_compressor:
    path: ../
  video_player:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
